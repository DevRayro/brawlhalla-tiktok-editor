# Pipeline dependencies (excluding torch / torchvision / SAM-2).
#
# torch + torchvision + SAM-2 are installed by the setup script with the right
# CUDA wheel for the user's GPU (cu128 for Blackwell, cu121 for older NVIDIA,
# or CPU/MPS on Mac). Keeping them out of this file lets `pip install -r` work
# regardless of the target hardware.

# Core
numpy==1.26.4
scipy==1.13.1
pyyaml==6.0.2
tqdm==4.66.5
psutil>=5.9
pynvml>=11.5

# Video / audio I/O.
# opencv-contrib-python is required for the CSRT tracker (legacy module).
opencv-contrib-python==4.10.0.84

# Transcription (word-level timestamps).
# faster-whisper bundles ctranslate2; on CUDA it uses the system cudnn libs.
faster-whisper==1.0.3
# huggingface_hub: 0.30+ falls back to file copies when Windows symlinks
# aren't permitted (common Windows non-admin case). 1.x changes the API in
# ways faster-whisper 1.0.3 doesn't handle, so cap below that.
huggingface_hub>=0.30,<1.0
requests==2.32.3

# SAM 2 dependencies (the SAM-2 package itself is installed by setup.{sh,bat}).
pillow>=10
hydra-core==1.3.2
iopath==0.1.10
omegaconf==2.3.0

# Local web UI (FastAPI server).
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.20

# Audio download (YouTube / SoundCloud / direct URLs). yt-dlp pulls MP3+
# metadata; ffmpeg (already required) re-encodes to 192k MP3 for clean handoff.
yt-dlp>=2024.10.0
